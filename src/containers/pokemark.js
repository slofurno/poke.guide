import React, { Component, PropTypes } from 'react'
import { connect } from 'react-redux'

class PokeMark extends Component {
  render() {

  }
}

export default connect(state => state, {})(PokeMark)
